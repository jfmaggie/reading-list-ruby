<% if item.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(item.errors.count, "error") %> prohibited this article from being saved.</h2>

  <ul>
    <% item.errors.full_messages.each do |error_message| %>
    <li><%= error_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form_for item, url: {action: action} do |form| %>
  <%= form.label :name%>
  <%= form.text_field :name%>

  <%= form.submit "#{action.capitalize} Item" %>
<% end %>
