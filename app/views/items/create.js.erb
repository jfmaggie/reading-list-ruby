<% if @item.errors.any? %>
  $("#item-errors").html("<%= escape_javascript(render partial: 'item_errors', locals: {list: @list, item: @item}) %>");
<% else%>
  window.location = window.location.pathname;
<% end %>
