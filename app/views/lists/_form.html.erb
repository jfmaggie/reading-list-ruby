<% if list.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(list.errors.count, "error") %> prohibited this article from being saved.</h2>

  <ul>
    <% list.errors.full_messages.each do |error_message| %>
    <li><%= error_message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<%= form_for list, url: {action: action} do |form| %>
  <%= form.label :name%>
  <%= form.text_field :name%>

  <%= form.submit "#{action.capitalize} List", class: "btn btn-primary" %>
<% end %>
